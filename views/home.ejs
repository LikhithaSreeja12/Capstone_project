<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Movie APP...</title> 
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@200;400;700&display=swap');
        *{
            box-sizing: border-box;
        }
        :root{
            --color1: #224b30;
            --color2: #693762;
        }
        body{
            background-color: var(--color1);
            font-family: 'Poppins', 'sans-serif';
            margin: 10;
            
            background-image: url('movie.jpg');
            background-repeat: no-repeat;
            background-attachment: fixed;  
            background-size: cover;
            background-size: 100% 100%;
            
        }
        header{
            padding:1rem; 
            text-align: center;
            background-color: var(--primary-color);
        }
      .intro{
            padding:0.1rem 1rem;
            display: flex;
            justify-content: flex-start;
            color: white;
        }
        .search{
            background-color: transparent;
            border: 2px solid var(--color1);
            padding:0.5rem 1rem;
            border-radius: 100px;
            font-size: 1rem;
            color: white;
            border-color: rgb(0, 153, 255);
            font-family: inherit;
        }
        .search:focus{
            outline:0;
            background-color: var(--color1);
        }
        .search::placeholder{
            color: #7378c5;
        }
        main{
            display:flex;
            flex-wrap: wrap;
            justify-content: center;
        }
        .movie{
            width: 300px;
            margin: 1rem;
            border-radius: 10px;
            background-color: var(--color2);
            position: relative;
            overflow: hidden;
        }
        .movie img{
            width: 100%;
        }
        .movie-info{
            color: white;
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 0.5rem 1rem 1rem;
        }
        .movie-info span{
            background-color: var(--color1);
            padding: 0.25rem 0.5rem;
            border-radius: 3px;
            font-weight: bold;
        }
        .overview{
            position:absolute;
            left:0;
            right:0;
            bottom:0;
            background-color: #fff;
            padding: 1rem;
            max-height: 100%;
            transform:translateY(101%);
            transition:transform 0.3s ease-in;
        }
        button {
            background-color: var(--color2);
            color: white;
            padding: 20px 0px;
            margin: 8px 0;
            border: none;
            cursor: pointer;
            width: 70%;
            opacity: 0.9;
        }
        .movie:hover .overview{
            transform:translateY(0)
        }
    </style>
</head>
 <body>
    <h2 class="intro">Hey Hello .</h2>
    <% for(var i=0;i<userData.length;i++) { %>
       <h3 class="intro">Welcome, <%= userData[i].name %> <br> </h3>
    <% } %>
<hr>
    <header>
        <div>
            <p style="background-color: var(--color2) ;color: white"></p>
            <form action="movienamesearch" method="GET">
                <input type="text" placeholder="Search" name="moviename" class="search">
            </form>
            
        </div>
    </header>

    <div style="text-align:center">
        <p style="background-color: var(--color2) ;color: rgb(170, 23, 23)"></p>
    </div>
    </body>
</html>